[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ev-recognition"
version = "0.1.0"
description = "Emergency vehicle visual/audio recognition with yield controller."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
