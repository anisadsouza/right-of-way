# Audio Dataset Folder

Create one folder per class:

```text
siren/
ambient/
```

Put `.wav`, `.mp3`, `.flac`, `.ogg`, or `.m4a` clips inside each class folder before running:

```bash
python -m evr.train_audio --data data/audio
```
